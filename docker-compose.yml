services:
  radarr-sonarr-mcp:
    build: .
    container_name: radarr-sonarr-mcp
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - MCP_TRANSPORT=streamable-http
      - MCP_SERVER_PORT=3000
      - MCP_HOST=0.0.0.0
    restart: unless-stopped
